# Kripto Botu — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core data / numerics
numpy>=1.26.0,<3.0
pandas>=2.1.0,<3.0
scipy>=1.11.0

# ── Async HTTP & networking
aiohttp>=3.9.0,<4.0
aiosignal>=1.3.0
aiohappyeyeballs>=2.3.0
frozenlist>=1.4.0
multidict>=6.0.0
yarl>=1.9.0

# ── Binance / exchange client
python-binance>=1.0.19
websocket-client>=1.6.0
websockets>=12.0

# ── Telegram bot
python-telegram-bot>=22.0,<23.0
httpx>=0.25.0
h11>=0.14.0
httpcore>=1.0.0

# ── Sync HTTP (used by requests-based fallback sender and deleteWebhook)
requests>=2.31.0
urllib3>=2.0.0
certifi>=2023.7.22
charset-normalizer>=3.3.0
idna>=3.4

# ── Charting (optional — set DISABLE_CHARTING=1 to skip)
matplotlib>=3.8.0
Pillow>=10.0.0
playwright>=1.40.0
contourpy>=1.2.0
fonttools>=4.44.0
cycler>=0.12.0
pyparsing>=3.1.0

# ── Environment & configuration
python-dotenv>=1.0.0

# ── Date / time utilities
python-dateutil>=2.8.0
pytz>=2023.3
tzdata>=2023.3
tzlocal>=5.0
dateparser>=1.2.0

# ── Crypto & security
pycryptodome>=3.19.0

# ── Market data enrichment (optional — used by RiskManager market-cap filter)
pycoingecko>=3.1.0

# ── Miscellaneous async / runtime
six>=1.16.0
regex>=2023.10.3
typing_extensions>=4.8.0
attrs>=23.1.0
anyio>=4.0.0
greenlet>=3.0.0
propcache>=0.2.0
sniffio>=1.3.0
pyee>=11.0.0
packaging>=23.2
kiwisolver>=1.4.5
